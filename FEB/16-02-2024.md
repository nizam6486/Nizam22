File system:

=>file sysytem is used for to manage files

=>The file system is used for to save and retrive the                        data.

=>operating system stores files and directories

=> system configuration files = folder A

=> user files = folder B

=> log file = folder C like that we can understand the file sysytems

=> There are many diffrent types of file systems. There are so many Improvement in file sysytems with new release s of new operating sysytem and new each file systems
ex: ext3, ext4, xfs, NTFS, FAT,

=> /boot: cantains file that is used by boot loader           	(grub.cfg)
 
=>/root:root user home directory it is not same as/